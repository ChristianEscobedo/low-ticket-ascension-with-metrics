import type { EmailFrameworkSpec } from '../types';

/** Value-forward long-form: teach a complete idea, then a soft CTA. */
export const valueLongform: EmailFrameworkSpec = {
  label: 'Value-forward long-form',
  structure: `VALUE LONG-FORM STRUCTURE
- Open with the promise of what the reader will understand by the end.
- Teach one complete idea or framework, start to finish, with specifics.
- Use short subheads or numbered steps so it stays scannable.
- Close with a soft CTA that continues the work, never a hard pitch.`,
  lengthTarget: 'long',
  styleNote: `Generous and genuinely useful on its own. The reader should get real
value even if they never buy. The CTA is an invitation, not pressure.`,
};
