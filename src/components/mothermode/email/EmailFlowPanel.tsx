/**
 * @deprecated Use `EmailFlowDashboard` instead.
 *
 * Kept as a thin re-export so any lingering imports keep working after the
 * View flow + Dashboard merge. The interactive canvas (with trigger
 * programming UI) lives entirely in `EmailFlowDashboard`.
 */
export { default } from './EmailFlowDashboard';
