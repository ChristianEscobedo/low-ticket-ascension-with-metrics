2961| const [uploadJob, setUploadJob] = useState<{
3462| function patchClipLive(id: string, partial: Partial<ReelClip>) {
3570| patchClipLive(clipId, { durationSec: dur });
3608| patchClipLive(clipId, { url: ready ? url : localBlob!, durationSec: localDur || 5 });
4020| const previewSrc =
8347| {uploadJob && (
8350| {uploadJob.phase}
8355| style={{ width: `${Math.max(4, uploadJob.pct)}%` }}
8359| {uploadJob.name} · {uploadJob.pct}%
8648| {uploadJob && (
8651| {uploadJob.phase}
8656| style={{ width: `${Math.max(4, uploadJob.pct)}%` }}
8660| {uploadJob.name} {uploadJob.pct}%
8961| {uploadJob?.blobUrl && (
8963| src={uploadJob.blobUrl}
8970| {uploadJob && (
8972| <p className="mb-1.5 text-[11px] font-semibold text-bone">{uploadJob.phase}</p>
8976| style={{ width: `${Math.max(4, uploadJob.pct)}%` }}